// Browser stub for Node built-ins (stream, fs, crypto) used by xlsx-js-style.
// The library checks properties like `stream.Readable` and skips the branch
// when they're falsy, so an empty module is enough.
export default {};
