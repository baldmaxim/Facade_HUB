-- Удаляет устаревшую таблицу vor_templates (плоские строки шаблонов).
-- Была нужна для старой функции "Заполнить шаблонами" на странице ВОРа объекта.
-- Вся работа теперь делается через новый движок:
--   - src/lib/vorTemplates.js — 36 шаблонов в коде
--   - vor_custom_templates — пользовательские шаблоны (fallback)
--
-- ⚠️ Перед запуском убедись, что в таблице нет важных данных.
-- Посмотреть что там: SELECT COUNT(*) FROM vor_templates;

DROP TABLE IF EXISTS vor_templates CASCADE;
